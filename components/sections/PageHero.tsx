import { Container } from "@/components/ui";
export function PageHero({ title, intro }: { eyebrow?: string; title: string; intro: string }) { return <header className="textile-surface relative overflow-hidden border-b border-[#dedbd4] bg-[linear-gradient(135deg,#fbfaf7,#e9eef1)] pb-20 pt-28 text-[#1d1d1f] md:pt-32"><div className="absolute -right-24 top-16 size-[420px] rounded-full bg-[#17486a]/8 blur-3xl"/><Container><h1 className="display max-w-5xl">{title}</h1><p className="mt-6 max-w-2xl text-xl leading-relaxed text-[#555b60]">{intro}</p></Container></header>; }
export function EditorialBlocks({ blocks }: { blocks: { title: string; text: string }[] }) { return <section className="section"><Container><div className="grid gap-px overflow-hidden rounded-3xl bg-[#d2d2d7] md:grid-cols-2">{blocks.map((b,i)=><article className={`bg-white p-8 md:p-12 ${i===0?"md:col-span-2":""}`} key={b.title}><span className="text-xs font-bold text-[#47738f]">{String(i+1).padStart(2,"0")}</span><h2 className="mt-8 text-3xl font-semibold tracking-tight">{b.title}</h2><p className="copy mt-5">{b.text}</p></article>)}</div></Container></section>; }
