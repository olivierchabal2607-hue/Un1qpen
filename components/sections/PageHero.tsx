import { Container } from "@/components/ui";
export function PageHero({ eyebrow, title, intro }: { eyebrow: string; title: string; intro: string }) { return <header className="bg-[#182b3a] pb-24 pt-40 text-white"><Container><p className="eyebrow !text-[#d5a184]">{eyebrow}</p><h1 className="display mt-7 max-w-5xl">{title}</h1><p className="mt-8 max-w-2xl text-xl leading-relaxed text-white/65">{intro}</p></Container></header>; }
export function EditorialBlocks({ blocks }: { blocks: { title: string; text: string }[] }) { return <section className="section"><Container><div className="grid gap-px overflow-hidden rounded-3xl bg-[#d9d7d1] md:grid-cols-2">{blocks.map((b,i)=><article className={`bg-white p-8 md:p-12 ${i===0?"md:col-span-2":""}`} key={b.title}><span className="text-xs font-bold text-[#a46f52]">{String(i+1).padStart(2,"0")}</span><h2 className="mt-8 text-3xl font-semibold tracking-tight">{b.title}</h2><p className="copy mt-5">{b.text}</p></article>)}</div></Container></section>; }
